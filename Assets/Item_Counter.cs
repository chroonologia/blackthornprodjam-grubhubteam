using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Item_Counter : MonoBehaviour
{
    private float Relic = 0;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //If player picks up Relic then gains debuff
        //If(Relic = 1)
        //{
        //Playermovement -= Playermovement * 0.15;
        //}
        //If(Relic = 2)
        //{
        //Playermovement -= Playermovement * 0.2;
        //PlayerHealth -= 0.5 * Time.deltaTime;
        //}
        //If(Relic = 3)
        //{
        //Playermovement -= Playermovement * 0.3;
        //PlayerHealth -= 1 * Time.deltaTime;
        //}
        //If(Relic = 4)
        //{
        //Playermovement -= Playermovement * 0.6;
        //PlayerHealth -= 3 * Time.deltaTime;
        //}
        //If(Relic = 4)
        //{
        //Playermovement -= Playermovement * 0.8;
        //PlayerHealth -= 5 * Time.deltaTime;
        //}
        //If(Relic = 5)
        //{
        //Kill Player
        //Restart GameScene
        //}
    }
}
